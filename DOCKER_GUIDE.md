# 🐳 Руководство по запуску и остановке приложения через Docker

## ✅ Приложение успешно запущено!

### 📍 Доступ к приложению:

- **Frontend**: http://localhost:3000
- **API**: http://localhost:8000
- **API Docs**: http://localhost:8000/docs
- **Nginx**: http://localhost:80

---

## 🚀 Команды для управления приложением

### Запуск приложения

```bash
# Перейти в директорию проекта
cd /Users/stepanpopko/Documents/GitHub/Alfa-hak

# Запустить все сервисы в фоновом режиме
docker-compose up -d

# Применить миграции базы данных (только при первом запуске или после изменений схемы)
docker-compose exec api alembic upgrade head

# (Опционально) Заполнить базу тестовыми данными
docker-compose exec api python -m app.scripts.seed
```

### Просмотр логов

```bash
# Просмотр логов всех сервисов
docker-compose logs -f

# Просмотр логов конкретного сервиса
docker-compose logs -f api          # Backend API
docker-compose logs -f postgres     # PostgreSQL
docker-compose logs -f redis        # Redis
docker-compose logs -f celery-worker # Celery Worker
docker-compose logs -f frontend     # Frontend
docker-compose logs -f ollama       # Ollama LLM
```

### Проверка состояния контейнеров

```bash
# Показать все запущенные контейнеры
docker-compose ps

# Показать использование ресурсов
docker stats
```

---

## 🛑 Остановка приложения

### Вариант 1: Остановка без удаления данных (рекомендуется)

```bash
cd /Users/stepanpopko/Documents/GitHub/Alfa-hak

# Остановить все контейнеры, данные сохранятся
docker-compose stop

# Или остановить и удалить контейнеры (но сохранить volumes с данными)
docker-compose down
```

### Вариант 2: Полная остановка с удалением всех данных

```bash
cd /Users/stepanpopko/Documents/GitHub/Alfa-hak

# Остановить и удалить контейнеры + volumes (БД, Redis, Ollama)
docker-compose down -v

# ⚠️ ВНИМАНИЕ: Это удалит все данные из баз данных!
```

### Вариант 3: Остановка отдельных сервисов

```bash
# Остановить конкретный сервис
docker-compose stop api
docker-compose stop postgres
docker-compose stop redis

# Перезапустить конкретный сервис
docker-compose restart api
```

---

## 🔄 Перезапуск приложения

```bash
cd /Users/stepanpopko/Documents/GitHub/Alfa-hak

# Перезапустить все сервисы
docker-compose restart

# Перезапустить конкретный сервис
docker-compose restart api
```

---

## 🧹 Очистка Docker

### Освободить место на диске

```bash
# Удалить неиспользуемые Docker ресурсы
docker system prune -a

# Удалить неиспользуемые volumes
docker volume prune
```

---

## 🆘 Troubleshooting

### Проблема: Порт уже занят

```bash
# Найти процесс, использующий порт (например, 8000)
lsof -i :8000

# Убить процесс
kill -9 <PID>
```

### Проблема: Контейнеры не запускаются

```bash
# Пересоздать контейнеры
docker-compose down
docker-compose up -d --build

# Проверить логи
docker-compose logs -f
```

### Проблема: Ошибки базы данных

```bash
# Применить миграции заново
docker-compose exec api alembic downgrade base
docker-compose exec api alembic upgrade head
```

---

## 📦 Запущенные сервисы

| Сервис | Контейнер | Порт | Описание |
|--------|-----------|------|----------|
| PostgreSQL | alfacopilot-postgres | 5432 | База данных с pgvector |
| Redis | alfacopilot-redis | 6379 | Кэш и очереди |
| Ollama | alfacopilot-ollama | 11434 | Локальный LLM сервер |
| LiteLLM | alfacopilot-litellm | 4000 | LLM прокси |
| Backend API | alfacopilot-api | 8000 | FastAPI приложение |
| Celery Worker | alfacopilot-celery | - | Обработка фоновых задач |
| Frontend | alfacopilot-frontend | 3000 | Vue.js интерфейс |
| Nginx | alfacopilot-nginx | 80 | Reverse proxy |

---

## 🔧 Разработка

### Запуск только инфраструктуры (PostgreSQL, Redis)

Если хотите разрабатывать локально:

```bash
# Запустить только БД и Redis
docker-compose up -d postgres redis ollama litellm

# Затем запустить backend локально
cd backend
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### Применение изменений

```bash
# После изменения кода - контейнеры автоматически перезагрузятся (--reload)

# После изменения requirements.txt или Dockerfile
docker-compose build api celery-worker
docker-compose up -d api celery-worker

# После изменения моделей БД
docker-compose exec api alembic revision --autogenerate -m "Description"
docker-compose exec api alembic upgrade head
```

---

## 📝 Примечания

- ✅ Все сервисы запущены в фоновом режиме (`-d`)
- ✅ Данные PostgreSQL, Redis и Ollama сохраняются в Docker volumes
- ✅ Backend и Frontend монтируют локальные директории для hot-reload
- ✅ Файл `.env` содержит все переменные окружения

**Сделано!** Приложение готово к работе! 🚀
