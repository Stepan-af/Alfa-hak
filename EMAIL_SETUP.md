# 📧 Настройка Magic Link Email (Бесплатно)

## Варианты бесплатной отправки email

### ✅ Вариант 1: Gmail SMTP (Рекомендуется для разработки)

**Преимущества:**
- ✅ Полностью бесплатно
- ✅ До 500 писем в день
- ✅ Быстрая настройка (5 минут)
- ✅ Высокая надежность

**Ограничения:**
- ⚠️ Нужен Google аккаунт
- ⚠️ Требуется двухфакторная аутентификация

#### Пошаговая инструкция:

**Шаг 1: Включите двухфакторную аутентификацию**
1. Откройте https://myaccount.google.com/security
2. Найдите раздел "Двухэтапная аутентификация"
3. Нажмите "Начать" и следуйте инструкциям

**Шаг 2: Создайте пароль приложения**
1. Откройте https://myaccount.google.com/apppasswords
2. Выберите:
   - **Приложение:** Почта
   - **Устройство:** Другое (введите "Alfa Copilot")
3. Нажмите "Создать"
4. **ВАЖНО:** Скопируйте сгенерированный 16-значный пароль (вида: `xxxx xxxx xxxx xxxx`)

**Шаг 3: Обновите `.env` файл**

Откройте файл `.env` в корне проекта и обновите следующие строки:

```bash
# Email (Magic Link) - Gmail SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=xxxx xxxx xxxx xxxx  # Ваш пароль приложения (16 символов)
SMTP_FROM=your-email@gmail.com
```

**Пример:**
```bash
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=stepanpopko@gmail.com
SMTP_PASSWORD=abcd efgh ijkl mnop
SMTP_FROM=stepanpopko@gmail.com
```

**Шаг 4: Отключите режим разработки (опционально)**

В файле `.env` измените:
```bash
DEBUG=false  # Было: DEBUG=true
```

**Шаг 5: Перезапустите API контейнер**
```bash
docker restart alfacopilot-api
```

**Шаг 6: Проверьте отправку**
```bash
# Посмотрите логи API
docker logs alfacopilot-api --tail 20

# Попробуйте войти через magic link
# http://localhost:3000 -> введите email
```

---

### ✅ Вариант 2: Mailgun (Для продакшена)

**Преимущества:**
- ✅ 5,000 писем в месяц бесплатно
- ✅ Профессиональный сервис
- ✅ Хорошая доставляемость
- ✅ Подробная статистика

**Ограничения:**
- ⚠️ Требуется верификация домена (для более 100 писем/день)
- ⚠️ Нужна кредитная карта (не списывается)

#### Пошаговая инструкция:

**Шаг 1: Регистрация**
1. Откройте https://signup.mailgun.com/new/signup
2. Зарегистрируйтесь (можно использовать временный домен)
3. Подтвердите email

**Шаг 2: Получите SMTP credentials**
1. В Mailgun Dashboard перейдите в **Sending** → **Domain Settings** → **SMTP credentials**
2. Или используйте Sandbox домен (для тестирования):
   - Домен: `sandboxXXXXXXXX.mailgun.org`
   - SMTP hostname: `smtp.mailgun.org`
   - Port: `587`

**Шаг 3: Создайте SMTP пользователя**
1. Перейдите в **Sending** → **Domain Settings** → **SMTP credentials**
2. Нажмите **Add New SMTP User**
3. Создайте логин (например: `alfacopilot@sandboxXXXX.mailgun.org`)
4. Сохраните пароль

**Шаг 4: Обновите `.env`**
```bash
# Email (Magic Link) - Mailgun
SMTP_HOST=smtp.mailgun.org
SMTP_PORT=587
SMTP_USER=postmaster@sandboxXXXXXXXX.mailgun.org
SMTP_PASSWORD=your-mailgun-smtp-password
SMTP_FROM=noreply@sandboxXXXXXXXX.mailgun.org
```

**Шаг 5: Добавьте авторизованных получателей (для Sandbox)**

⚠️ **Важно:** В бесплатном Sandbox режиме вы можете отправлять только на авторизованные email адреса!

1. Перейдите в **Sending** → **Domain Settings** → **Authorized Recipients**
2. Добавьте свой email адрес
3. Подтвердите через письмо

**Шаг 6: Перезапустите API**
```bash
docker restart alfacopilot-api
```

---

### ✅ Вариант 3: Yandex Mail (Для русских пользователей)

**Преимущества:**
- ✅ Бесплатно
- ✅ Поддержка кириллицы
- ✅ Надежная доставка в Россию

#### Инструкция:

**Шаг 1: Включите IMAP/SMTP**
1. Откройте https://mail.yandex.ru
2. Настройки → Все настройки → Почтовые программы
3. Включите "С сервера imap.yandex.ru по протоколу IMAP"

**Шаг 2: Создайте пароль приложения**
1. Откройте https://id.yandex.ru/security/app-passwords
2. Нажмите "Создать пароль приложения"
3. Выберите "Почта" и введите имя "Alfa Copilot"
4. Скопируйте пароль

**Шаг 3: Обновите `.env`**
```bash
# Email (Magic Link) - Yandex
SMTP_HOST=smtp.yandex.ru
SMTP_PORT=587
SMTP_USER=your-username@yandex.ru
SMTP_PASSWORD=your-app-password
SMTP_FROM=your-username@yandex.ru
```

**Шаг 4: Перезапустите API**
```bash
docker restart alfacopilot-api
```

---

## 🧪 Режим разработки (без email)

Если вы не хотите настраивать email прямо сейчас, можно оставить `DEBUG=true` в `.env`:

```bash
DEBUG=true
```

**В этом режиме:**
- ✅ Magic link печатается в консоль API контейнера
- ✅ Email не отправляется
- ✅ Можно скопировать ссылку из логов

**Как получить magic link:**
```bash
# 1. Попробуйте войти на http://localhost:3000
# 2. Введите любой email
# 3. Откройте логи API:
docker logs alfacopilot-api --tail 30

# Вы увидите:
# ================================================================================
# 🔑 MAGIC LINK (Copy this link to browser)
# 📧 Email: test@example.com
# 🔗 Link: http://localhost:3000/auth/verify?token=abc123...
# ================================================================================
```

---

## 🔧 Проверка настройки

### Тест отправки email:

Создайте файл `test_email.py` в папке `backend/`:

```python
import asyncio
from app.services.email import send_magic_link_email

async def test():
    await send_magic_link_email("your-email@gmail.com", "test-token-123")
    print("Email sent!")

asyncio.run(test())
```

Запустите:
```bash
docker exec -it alfacopilot-api python test_email.py
```

### Проверка логов API:

```bash
# Посмотреть последние 50 строк логов
docker logs alfacopilot-api --tail 50

# Следить за логами в реальном времени
docker logs alfacopilot-api -f
```

---

## 🚨 Решение проблем

### Ошибка: "535 Authentication failed"

**Причина:** Неверный пароль или логин

**Решение:**
1. Убедитесь, что используете **пароль приложения**, а не обычный пароль
2. Проверьте, что в пароле нет лишних пробелов
3. Для Gmail убедитесь, что включена двухфакторная аутентификация

### Ошибка: "Connection timeout"

**Причина:** Неверный SMTP host или port

**Решение:**
1. Проверьте `SMTP_HOST` и `SMTP_PORT` в `.env`
2. Убедитесь, что порт `587` не заблокирован файрволом
3. Попробуйте альтернативный порт `465` (SSL) и измените код

### Email не приходит (отправка успешна)

**Причина:** Письмо попало в спам или задержка доставки

**Решение:**
1. Проверьте папку "Спам"
2. Подождите 1-2 минуты (задержка доставки)
3. Для Gmail: добавьте `SMTP_FROM` в контакты

### Mailgun: "Free accounts are for test purposes only"

**Причина:** Sandbox режим ограничен

**Решение:**
1. Добавьте получателя в **Authorized Recipients**
2. Или верифицируйте свой домен (бесплатно)

---

## 📝 Рекомендуемая конфигурация

### Для локальной разработки:
```bash
DEBUG=true  # Magic links в логах, email не отправляется
```

### Для тестирования email:
```bash
DEBUG=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=your-email@gmail.com
```

### Для продакшена:
```bash
DEBUG=false
SMTP_HOST=smtp.mailgun.org
SMTP_PORT=587
SMTP_USER=postmaster@your-domain.com
SMTP_PASSWORD=your-mailgun-password
SMTP_FROM=noreply@your-domain.com
```

---

## 🎯 Быстрый старт (Gmail за 5 минут)

1. **Включите 2FA в Google:** https://myaccount.google.com/security
2. **Создайте пароль приложения:** https://myaccount.google.com/apppasswords
3. **Отредактируйте `.env`:**
   ```bash
   SMTP_HOST=smtp.gmail.com
   SMTP_PORT=587
   SMTP_USER=your-email@gmail.com
   SMTP_PASSWORD=xxxx xxxx xxxx xxxx
   SMTP_FROM=your-email@gmail.com
   DEBUG=false
   ```
4. **Перезапустите API:**
   ```bash
   docker restart alfacopilot-api
   ```
5. **Протестируйте:** http://localhost:3000 → Войти → Введите email → Проверьте почту

---

Готово! 🎉 Теперь magic links будут отправляться на email!
