# ⚡ Быстрый старт Alfa Copilot

Запуск проекта за 5 минут!

## Предварительные требования

- **Docker Desktop** установлен и запущен
- **8 GB RAM** минимум
- **10 GB** свободного места на диске

## Шаги

### 1. Клонировать репозиторий

```bash
git clone https://github.com/Stepan-af/Alfa-hak.git
cd Alfa-hak
```

### 2. Создать .env файл

```bash
cp .env.example .env
```

**Отредактировать .env** (минимальная конфигурация):

```env
SECRET_KEY=change-me-in-production-use-long-random-string
DEBUG=true
POSTGRES_PASSWORD=secure-password-here
DATABASE_URL=postgresql://alfacopilot:secure-password-here@postgres:5432/alfacopilot
```

### 3. Запустить все сервисы

```bash
docker-compose up -d --build
```

Первый запуск займёт **5-10 минут** (скачивание образов и LLM модели ~2GB).

### 4. Дождаться готовности

Проверить статус:

```bash
# Должны быть все контейнеры Up
docker-compose ps

# Проверить что LLM модель скачалась
docker logs alfacopilot-ollama | grep "llama3.2"
```

### 5. Применить миграции

```bash
docker exec alfacopilot-api alembic upgrade head
```

### 6. Открыть приложение

🎉 **Откройте браузер**: http://localhost:3000

## Первый вход

1. Введите **любой email** (example@test.com)
2. **Magic Link** будет в логах (т.к. DEBUG=true):
   ```bash
   docker logs alfacopilot-api | grep "Magic link"
   ```
3. **Скопируйте ссылку** и откройте в браузере
4. **Готово!** Вы вошли в систему

## Что дальше?

- 📊 **Финансы**: Добавьте транзакции или загрузите CSV
- 📄 **Документы**: Создайте договор из шаблона
- 📱 **Маркетинг**: Сгенерируйте пост для соцсетей с AI
- ✅ **Задачи**: Создайте задачи на Kanban доске
- 💬 **Чат**: Поговорите с AI ассистентом

## Полезные команды

```bash
# Логи сервисов
docker-compose logs -f api
docker-compose logs -f frontend

# Перезапуск
docker-compose restart

# Остановка
docker-compose down

# Полная очистка и пересборка
docker-compose down -v
docker-compose up -d --build
```

## Проблемы?

### Frontend не открывается
```bash
docker-compose restart frontend
```

### API ошибки
```bash
docker logs alfacopilot-api -f
docker exec alfacopilot-api alembic current
```

### LLM не отвечает
```bash
# Проверить модель установлена
docker exec alfacopilot-ollama ollama list

# Должна быть llama3.2:3b (2GB)
```

## Подробная документация

- **Полная инструкция**: [README.md](./README.md)
- **Настройка Email**: [EMAIL_SETUP.md](./EMAIL_SETUP.md)
- **Настройка LLM**: [LLM_SETUP.md](./LLM_SETUP.md)
- **Docker гайд**: [DOCKER_GUIDE.md](./DOCKER_GUIDE.md)

---

**Успехов! 🚀**
